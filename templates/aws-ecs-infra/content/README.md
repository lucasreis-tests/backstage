# ${{ values.name }} — AWS Infrastructure

> Auto-generated by Backstage using the reference architecture template.

## Architecture

| Component | Details |
|---|---|
| VPC | `10.0.0.0/16`, 3 AZs, public/private/database subnets |
| ECS Fargate | ${{ values.ecs_cpu }} CPU, ${{ values.ecs_memory }} MiB RAM, auto-scaling 2-6 |
| RDS PostgreSQL | 16, ${{ values.db_instance_class }}, Multi-AZ, encrypted |
| ALB | HTTP:80, health check `/healthcheck` |
| WAF v2 | Rate limit + AWS Managed Rules |
| Monitoring | CloudWatch alarms → SNS |

## Quick Start

```bash
# 1. Init
terraform init

# 2. Review
terraform plan

# 3. Apply
terraform apply

# 4. Push your container image
aws ecr get-login-password --region ${{ values.aws_region }} | \
  docker login --username AWS --password-stdin $(terraform output -raw ecr_repository_url)

docker build -t ${{ values.name }} .
docker tag ${{ values.name }}:latest $(terraform output -raw ecr_repository_url):latest
docker push $(terraform output -raw ecr_repository_url):latest

# 5. Deploy
aws ecs update-service \
  --cluster ${{ values.name }}-cluster \
  --service ${{ values.name }}-service \
  --force-new-deployment \
  --region ${{ values.aws_region }}
```

## Destroy

```bash
terraform destroy
```
