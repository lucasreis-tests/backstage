apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ${{ values.dev_namespace }}
namePrefix: dev-

resources:
  - ../../base

patches:
  - path: replicas.yaml
  - path: configmap.yaml
