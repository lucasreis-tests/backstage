apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ${{ values.prod_namespace }}
namePrefix: prod-

resources:
  - ../../base
  - hpa.yaml
  - pdb.yaml

patches:
  - path: replicas.yaml
  - path: configmap.yaml
